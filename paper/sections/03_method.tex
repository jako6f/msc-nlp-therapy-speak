% Must include: data source (Common Crawl), time slicing, sampling, filtering,
% ASD disambiguation window, domain cap, deduping, and evaluation of noise/recall.
% Also include reproducibility details (seeds, manifests, versions).

\section*{No-copy--paste outputs}
Figures are generated by code into \texttt{../reports/figures/} and included via \texttt{\textbackslash includegraphics}. Tables are generated by code into \texttt{../reports/tables/} and included via \texttt{\textbackslash input}.
